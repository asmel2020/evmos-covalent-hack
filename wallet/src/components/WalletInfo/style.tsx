import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container:{
        justifyContent: 'center',
        alignItems: 'center',
    },
    address:{
        fontSize:20,
        marginTop:5
    },
    balance:{
        fontSize:20,
        marginTop:5
    }
})